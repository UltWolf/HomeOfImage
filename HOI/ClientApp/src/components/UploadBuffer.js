import React, { Component } from 'react';

export class UploadBuffer extends Component {
    static displayName = UploadBuffer.name;

    render() {
        return (

            <h1>Or u can just input image by ctrl+v  to this field</h1>

        );
    }
}